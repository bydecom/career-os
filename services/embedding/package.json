{
  "name": "@career-os/embedding",
  "version": "1.0.0",
  "description": "Embedding provider — wraps the Google Gemini text-embedding API via plain fetch (zero SDK dependency).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
