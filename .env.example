# Qdrant (local via `docker compose up -d qdrant`)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Google Gemini — embeddings (text-embedding-004) + chat verbalize (REST, no SDK)
GEMINI_API_KEY=
# Optional chat model override (default: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash
